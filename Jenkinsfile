pipeline {
    agent any

    
    stages {
        stage('Checkout') {
            steps {
                // Checkout code from Github repository
                git url: 'https://github.com/preet0807/DevOps.git', branch: 'main'
            }
        }
    }
)
        
